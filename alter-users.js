require('dotenv').config(); const { Pool } = require('pg'); const pool = new Pool({ user: process.env.DB_USER, host: process.env.DB_HOST, database: process.env.DB_NAME, password: process.env.DB_PASSWORD, port: process.env.DB_PORT }); pool.query('ALTER TABLE users ADD COLUMN IF NOT EXISTS nik VARCHAR(255); ALTER TABLE users ADD COLUMN IF NOT EXISTS jabatan VARCHAR(255);').then(function(){ console.log('Columns added successfully'); pool.end(); }).catch(function(err){ console.error(err); pool.end(); });  
